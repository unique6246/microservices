# All configuration is managed in application.yml using environment variables.
# Sensitive credentials (SMTP_PASS, DB_PASS, etc.) must be provided via .env or environment.
# This file is intentionally left empty ? do NOT add credentials here.
